{
  "name": "chatbot-project",
  "version": "1.0.0",
  "description": "Projet de chatbot déployé sur Netlify",
  "scripts": {
    "build": "echo 'Build script not defined' && exit 0"
  },
  "dependencies": {}
}
